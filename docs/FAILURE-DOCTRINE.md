<!--
SPDX-FileCopyrightText: 2026 1337 · REL AZEUS · MΔSTERMIND
SPDX-License-Identifier: Apache-2.0
-->

# The won-battles ledger — every failure class we beat, and the law that keeps it dead

Operator mandate (2026-09-08): an identified failure may never recur.
This ledger is the machine-readable answer: one row per failure class,
its PERMANENT counter, and where that counter lives. Pilots: the firing
prompt's hard-rules block is the enforced summary; this is the study
text behind it. A failure that feels familiar probably has a row here —
read it before improvising. A NEW failure class earns a row in the same
commit that fixes it.

| # | Failure class (caught live) | Permanent counter | Where it lives |
| - | --- | --- | --- |
| 1 | Gate reported green while running zero tests | three-valued verdicts (confirmed/refuted/unverifiable) + implausibly-fast green demotes to unverifiable | engine gate + `convergence-gate.ts` |
| 2 | Red-main revert cascades on STALE verdicts (twice, once bidirectional) | no-auto-revert hard rule: verify verdict provenance, propose-don't-revert, default WAIT | firing prompt hard rules + `fly.ts` guard + debriefs 2026-09-06/07 |
| 3 | Broad commits sweeping a sibling's uncommitted/staged work | orphaned-diff-may-be-a-sibling rule, explicit-path adds, land-lock guards | firing prompt + `fly.ts` sync-back lock guard |
| 4 | Lane-vs-lane autoformat revert/reapply wars | autoformat WRITE is the landing's alone; lanes check only | firing prompt (v13) + boarded landing-step |
| 5 | A census trusting the instrument it audits (features barrel; i18n scanner) | censuses diff against the DIRECTORY/disk, never the discoverer alone | `chunks.test.ts` + boarded i18n-scanner fix |
| 6 | Scanner false-positives triggering damage (drive-`y:` from CSS regex) | guard-precision doctrine: negative corpus per scanner, matched text in evidence, scanner-wrong is a first-class hypothesis | `validate-no-personal-paths` + negative-corpus suites |
| 7 | CI-only failures discovered after push, layer by layer | PARITY landing gate runs CI's own ci:* checks before any push | `landing/execute.ts` (proved itself day one) |
| 8 | Landing push-leg failing silently (GitHub a day stale) | the landing PUSHES after a green merge and reports it as its own row — a failed push turns the line amber and names the remote, never claiming the land failed; non-FF says integrate-and-land-again | `GitVcs.pushBranch` + `landing/execute.ts` push leg + `landingExecuteResult` |
| 9 | Zombie server serving a stale build ("feature missing") | update-check self-reports the RUNNING build; restart after rebuild ritual | `/api/update-check` + boarded restart-detect |
| 10 | Stale lane worktree dirs silently killing lanes | ensureWorktree self-heals orphaned dirs | shipped, storm-reverted, reland boarded+focused |
| 11 | Duplicate fixes of the same red by two actors | check `git log` on the failing file before fixing; intent-claims | firing prompt (v13) |
| 12 | Piped exit codes masking failed git/gate commands | never pipe git/gate through head/tail in a `&&` chain | firing prompt (v13) |
| 13 | Fleet identity mis-signing a contributor's work (DCO) | human always signs as themself; engine hardcodes no identity; credit only in the Assisted-by trailer | `docs/ATTRIBUTION.md` §Signing & DCO |
| 14 | Core-bundle wall re-hit by every i18n slice | budgets as tripwires + dead-key pruning; structural: defer the locale table (invited) | `check-bundle-size` + VERDICT web-mtbodv7m |
| 15 | Operator-created tasks invisible below the board fold | report-born tasks are FOCUSED at birth | `applyReportTask` |
| 16 | Machine starvation under multi-lane gates (88-100% CPU) | below-normal lane priority + cross-lane gate semaphore + sustained-load planner | mercy 1-2 shipped (`spawn-flight.ts` + `gate-semaphore.ts`), mercy 3 shipped (`lucky-plan.ts`'s `CORES_PER_LANE` tightened 2→3 + `fly.ts` paints the CPU-bound reasoning line as a load hint, board web-mtsvcibf-bh6asp) |
| 17 | KEEPER Apply on stale cards | execute re-verifies live PR state and no-ops on merged/closed | `pr-review-execute.ts` `confirmPrNotOpen` |
| 18 | New file added without its census pin (taxonomy stubs ×2) | "a census completes the change" — same-commit pin updates | firing prompt (v13) + additive-only law |
| 19 | The same message posted twice by a retry after an apparent failure that had actually landed (PR #33's approval, 16s apart, 97% identical) | every `gh issue\|pr comment` runs through a guard that reads the thread first: a >=90%-similar message from this identity is a clean no-op | `flight/anti-flood.ts` + `flight/gh-exec.ts` (defaulted, census-pinned) |
| 20 | A third consecutive message from one identity stacking on a thread (issue #16) | at the ceiling the guard EDITS the tail into a dated `**Update:**` block instead of posting | `flight/anti-flood.ts` fold tier + `docs/ATTRIBUTION.md` §3 |
| 21 | An affordance offering an action that would do nothing ("Run KEEPER triage": 0 to accept, 6 already triaged) | disabled-with-reason: a control that cannot act says why, and what re-enables it | `issueTriageHasWork` + `issueTriageNothingToRunTip` |
| 22 | A refusal naming an action the operator had no way to take ("update the branch first"; a red check with no re-run) | every refusal that names an action ships that action as a button beside it | `update-branch` + `rerun-checks` verbs in `flight/human-merge.ts` |
| 23 | A refusal's reason wiped by the success-path re-render, leaving a dead-looking button | re-poll only when something actually changed; a refusal keeps its message and its button | `wirePrMaintainerAction`'s `changed` gate |
| 24 | Panel callbacks rendering into a torn-down document — a run with 9,865 passing tests failing on an unhandled rejection | self-polling panels bail when the document is gone | `renderPrReviewPanel` guard (sibling of issue-triage's `isConnected`) |
| 25 | A timeout fix applied to `testTimeout` only, while the flakier half — `beforeEach` setup — stayed at the 10s default | one budget covers both: `hookTimeout` raised alongside `testTimeout`, same reasoning | `vitest.config.ts` |

| 26 | A merge commit that CLAIMS both parents and keeps only one's tree (`-s ours`) — undetectable by anything in the repo | merge-integrity check: tree-identical-to-first-parent AND content missing at the tip = the parent's work was discarded | `ci:merge-integrity` + both corpora in `merge-integrity.test.ts` |
| 27 | Concurrent flights sharing one non-worktree checkout race at git-index/working-tree level (`ap-mtm4qzty-1`, reconfirmed 8×: swept commits, identical-content races, a hard reset silently erasing another firing's uncommitted edit) | `fly.ts`'s FLIGHT-VS-FLIGHT guards refuse the land-time bookends (primary-fallback start, sync-back call sites) when a sibling's lock is live; the edit-time window between those bookends has no lock and relies only on the reactive `3f4bd6c6` detect-before-commit rule | `fly.ts` land-time guards + firing-prompt hard rule (`packages/engine/src/prompt.ts`) — **open**: full edit-time isolation is the already-proposed operator call (every flight in its own worktree, per debriefs 2026-09-06/07/10) |

| 28 | Private operator context published in a PUBLIC commit log — the maintainer's own hardware state, the time of day it happened, and second-person session narration | commitlint reads the WHOLE message (subject, body, footer) and warns on wall-clock times, machine-state phrases and second-person address; warning-not-error so a mid-unit checkpoint still commits | `commitlint.config.js` `no-operator-private-context` + both corpora in `commit-privacy.test.ts` |
| 29 | A recovery merge landed a literal unresolved conflict block (`<<<<<<< HEAD` / `=======` / `>>>>>>> <branch>`) straight into a committed doc — `check-merge-integrity.mjs` couldn't see it, since that guard compares tree CONTENT across merge parents, not marker syntax within a file | `check-conflict-markers.mjs` scans every tracked file's lines for the marker shape (`<{7} `/`>{7} ` always flag; a bare `={7}` only counts paired with one of the other two, so an innocent Markdown divider stays clean) | `check-conflict-markers.mjs` + `check-conflict-markers.test.ts` (real-file regression against `BACKLOG-999-ARCHIVE.md`) |
| 30 | A reland restored a file's content verbatim from its original commit, but a census added AFTER that original commit was authored fired on it anyway — the restore read as already-proven work, not a fresh landing (`static-site.ts`'s detector marker, `2b2b3832`, needed one follow-up commit right after the `b3004cf7` reland) | reland IS a landing: run the full CURRENT gate + census sweep before committing restored content — never assume it clears checks added since the content was first authored | firing prompt (`firing-v15`) `## Hard rules` — the doctrine's earlier 0.7502 tail-position reading was stale (compaction elsewhere since `firing-v14` left ~490 chars of headroom under `prompt-position-audit.test.ts`'s 0.75 `CRITICAL_TAIL_FRACTION`), so the rule appended directly without a separate compaction pass |
| 31 | Two lanes of one fleet syncing back into the SAME checkout at once (the three-lane rung, 2026-09-19): lane 3's merge stopped on a content conflict in the generated debrief index and sat there while the escalation agent worked; lane 1's flight-end sync-back read those conflict markers as "uncommitted changes" and refused — its commits stranded on the lane branch, the merge itself still unresolved, and nothing was ever wrong with the tree | one sync-back at a time per repository: `syncWorktreeBranch` takes a lockfile in the git common dir (`autopilot-sync-back.lock`, a `FileInstanceLock` — pid inside, a dead holder reclaimed) and a second lane WAITS for it instead of judging a tree another lane is mid-way through — the flight-end sync-back, the last chance before a lane's commits strand, waits 15 min (polled every 2 s, long enough for a merge, a rerere replay and an escalation agent), the retried per-firing and launch-time sync-backs 2 min so one lane's escalation never parks the rest; a lock that cannot be taken at all (read-only `.git`, a handle held for a moment) is reported and the sync-back runs unguarded rather than throwing; the generated debrief index joins the `merge=union` absorbers since every lane regenerates it with its own row and the next regeneration re-sorts | `withSyncBackMutex`/`syncBackLockPath` in `packages/engine/src/adapters/worktree.ts`, `.gitattributes` `docs/debriefs/README.md merge=union` |
| 32 | Every lane's convergence gate ran in the ONE live checkout — the tree every other lane's sync-back rewrites (four-lane rung, 2026-09-19): two flight-end gates went red on `pnpm run test` while a sibling's merge and full gate rewrote the checkout under them, and the same head passed the whole suite alone; a "merge interaction" that was a race, not a merge | the merged head is gated in the lane's PRIVATE worktree: fast-forward the lane to the merged head (a clean lane is always its strict ancestor), run the gate there, and a lane that cannot be forwarded says so and persists the gap as a convergence alarm instead of gating a shared tree; only the merge-escalation agent's validation still runs in the live checkout, because an in-progress merge exists nowhere else | `forwardLaneToMergedHead`/`gateMergedHead` in `apps/dashboard/src/fly.ts`, census `merged-head-gating.test.ts` |
| 33 | A CONVERGENCE RED named its check (`pnpm run test`) but not the test that broke, although the gate had already captured the failing command's last lines — the operator reran a 25-minute suite to learn what the log already knew | the failing command's `outputTail` travels with the alarm: indented under the red line in the flight log, persisted in the `convergence-red` event, and quoted in the anomaly chip (its failure-marked lines, at most three, cut to a chip's width) | `convergence-gate.ts` `indentedTail`, `anomalies.ts` `quotedFailure`, `persisted-events.ts` `outputTail`; mutation config `dashboard-convergence-gate` |
| 34 | A sync-back published whatever sat at the lane's HEAD — including a commit the gate had refused to judge (`unverifiable`: a second file left uncommitted beside it, nothing reverted). The flight branch's formatter check went red one lane later, and the landing would have refused the rung (four-lane rung, 2026-09-19) | only a verified head is published: the lane carries one bit across firings — has a gate judged the tree at HEAD green — moved by each record (green sets it, a revert keeps it, a checkpoint clears it — an unfinished unit is never published, whatever its impacted-tests telemetry gate said — and so does an unverifiable commit), persisted by sha, and consulted by every sync-back: per firing, at flight end (which then takes the stranded-work path: log, event, inbox task) and at the next launch's catch-up, where a parked head stays parked only while it is the same commit | `apps/dashboard/src/flight/lane-head.ts` + its census over fly.ts; mutation config `dashboard-lane-head` |
| 35 | A gate step killed by the ten-minute timeout under five lanes was reported as a CONVERGENCE RED "merge interaction" — `pnpm run test` needs five minutes idle and fifteen to twenty-five under fleet load, and a kill is not a verdict (five-lane rung, 2026-09-19) | a crashed gate result is UNJUDGED, never red: logged with the crash reason, persisted under a `(crashed, no verdict)` label so the chip stays loud but honest; and every flight gate step carries a thirty-minute ceiling, still bounded for a command that truly hangs | `convergence-gate.ts` crash branch, `FLIGHT_GATE_STEP_TIMEOUT_MS` in fly.ts (census in `merged-head-gating.test.ts`) |
| 36 | The stranded-work inbox task passed `dimension: 'process'`, a value the tasks table's CHECK constraint rejects; `createTask` answered `false`, the caller never looked, and the log said "task filed" for three weeks while none existed (found the first time the publication rule withheld a head, 2026-09-19) | the store drops an unknown dimension and says so instead of dropping the task; the caller files without a bucket and prints only what the store confirmed — a logged claim is a claim the code checked | `createTask` in `packages/store/src/mutate.ts`, the stranded escalation in fly.ts (census in `lane-head.test.ts`) |
| 37 | Six lanes' gates and firings running the suite at once pushed the real-git and real-store tests past vitest's 30-second per-test budget; a lane's flight-end gate went red on three timeouts that are seconds on an idle machine — a loaded run read as a broken merge (six-lane rung, 2026-09-19) | the budget is sized for the load it must survive, not for the idle case: 120 seconds for tests and hooks alike (row 25's pairing kept), which hides nothing — a genuinely hung test still fails, two minutes later; and no per-test or per-file override may sit BELOW it (the eight-lane round found six `30000` and one `60000` still in the onboarding e2e file and a `90_000` in a11y — each one the row-25 half-applied shape again) | `vitest.config.ts`, the onboarding e2e and a11y suites |
| 38 | A head parked by one flight (a checkpoint, an unverifiable commit) stayed parked through the next: the standing RESUME CHECK in the firing prompt did not get five lanes to finish their units, each firing bailed at a dollar, and the lanes were dead ends filing the same inbox task again (seven-lane rung, 2026-09-19) | at launch a still-parked head is kept under `refs/autopilot/parked/<lane branch>/<sha8>` and the lane branch is reset onto the shared tip — only a clean lane, only after the ref exists — so nothing is lost and the lane works again; the flight-end refusal and its inbox task name the ref in advance | `parkAsideWorktreeHead` in `packages/engine/src/adapters/worktree.ts`, the launch wiring in fly.ts (census in `lane-head.test.ts`) |
| 39 | Six of eight firings in the eight-lane round ended `exit 1` with no result envelope, and nothing anywhere said why: the CLI adapter dropped the process's stderr on the close path, the record carried `isError: null, stopReason: null` and nothing else, and the flight log printed no line at all — a death with no cause is a throughput problem nobody can diagnose (2026-09-19) | the stderr tail (last 600 characters) rides as `deathTail` on the envelope facts, into the firing record and its persisted event, and the loop prints one line per envelope-less firing naming the exit code and the tail — `nothing on stderr` when there was none, never silence | `stderrTail` in `adapters/claude-cli.ts`, `EnvelopeFacts.deathTail`, the loop's summary line |
| 40 | Twenty-one of thirty-four firings in one day were killed at exactly thirty minutes by the CLI's wall-clock cap — sized for a lone flight, blind to a fleet where the same firing waits on gate slots and shares one disk — and on Windows the kill closed with `code 1, signal null`, so the signal-based inference reported every one as an ordinary crash; agents also ran the whole test suite inside their firings, twenty minutes each under load (2026-09-19) | the caps are the adapter's OWN timers, reported as a cap death on every platform with a note naming the cap and the elapsed time; the wall clock is ninety minutes and a twenty-minute IDLE cap catches a genuinely hung child far sooner; the firing prompt forbids running the whole suite (the gate does that) and names the wall clock; both caps are announced at takeoff and tunable (`AUTOPILOT_CLI_TIMEOUT_MS`, `AUTOPILOT_CLI_IDLE_TIMEOUT_MS`) | `adapters/claude-cli.ts` (`capDeathNote`, `DEFAULT_CLI_IDLE_TIMEOUT_MS`), `flight/budget.ts`, the TURN BUDGET section of `prompt.ts` (firing-v16) |
| 41 | Every failure class of the 2026-09-19 lane ladder had a precondition visible before takeoff — a dirty live checkout (every sync-back refuses), a missing git identity (every commit fails), no `claude` on the path, a full disk, a lane left with uncommitted leftovers, a stale build, a fleet wider than one disk feeds — and a user found each one only after paying for the flight | PREFLIGHT: one go/no-go every launch path shares (the Fly button, the fleet launcher, the fleet watchdog all pass through `FlightRunner.start()`); a blocking check refuses the flight with its advice as the message, a warning lets it fly and says what to expect, and `pnpm dashboard:doctor <folder>` prints the same report from the terminal | `flight/preflight.ts` (pure, mutation config `dashboard-preflight`), `flight/preflight-facts.ts`, the runner's `preflight` dep |
| 42 | The FINISH-LINE extension — the one mechanism that lets a firing killed mid-unit pack its work up — was gated on the session id of the RESULT envelope, which a killed attempt by definition never sends. Every one of the twenty-two wall-clock deaths in a day skipped the rescue for want of an id that had been on the wire since the first stream event, and the agent was never told the wall clock it was dying on (2026-09-19) | the session id is read from the STREAM (`sessionIdFromEvent`, carried on `ModelResponse.sessionId`), so a killed attempt is resumable and gets its bounded extension with a fresh clock; and the firing prompt names the wall clock in minutes beside the turn cap (firing-v17) | `stream.ts`, `adapters/claude-cli.ts`, `firing.ts`'s `ownSessionId`, `prompt.ts` `turnBudgetSection` |
| 43 | A red nightly mutation sweep could not be told apart from a killed one. The runner caught every `stryker run` throw with a bare `catch {}` and printed `FAILED — <config>` plus a bare tally, so a config that the OS killed at eighty of four hundred and twenty-six mutants — no score produced, nothing surviving — read exactly like a config with a live survivor; the tally never named which config failed either, so the answer lived somewhere in a six-shard log (2026-09-21) | the throw is read, not discarded: a `signal` says the process was killed and names the environment, exit 1 says a mutant survived, any other code says stryker never scored — and the closing summary lists every failure BY NAME with its reason under the tally | `scripts/ci/run-all-mutation.mjs` (`mutationFailureReason`, `formatFailureSummary`), `test/tooling/run-all-mutation.test.ts` |
| 44 | The changelog seed fired only on TOTAL silence, so a `[Unreleased]` section written once and then left behind was published as-is. The section held two bullets; ninety-nine commits had landed since it was written, and ninety-seven of them would have shipped in a version that never mentioned them. Two bullets are not silence, so nothing rescued it and nothing warned (caught by hand one step before the cut, 2026-09-21) | the cut COMPLETES a section instead of trimming or refusing it: every hand-written line survives untouched, and each `feat`/`fix`/`perf` commit the section does not name verbatim is appended under `### Also in this release`. An empty section is still seeded whole, so silence and partial silence are both covered | `release.ts` (`unreleasedGaps`, `unreleasedBody`, `cutChangelogRelease`), mutation config `engine-release`, `docs/RELEASING.md` |
| 45 | The release notes counted COMMITS, not changes. A feature that shipped, was reverted and shipped again leaves its subject twice in the log, so the generated tag body announced one feature as two — and the same blind spot reads the other way, since `Revert "X"` leaves X in the log and notes built from raw subjects would announce a feature that was taken back out before the release (caught reading the notes preview, 2026-09-21) | one line per change: each subject is named once, and a revert cancels one landing of the commit it names — reverted-then-reapplied nets to one line, reverted-and-left nets to none. The changelog cut and the tag body read the same list, so the two can never tell different stories | `release.ts` (`releaseWorthySubjects`, used by `groupedReleaseNotes` and `unreleasedGaps`), mutation config `engine-release` |
| 46 | KEEPER overruled the person who writes its rules, twice on one issue. #5 — the planned static-site sample, filed by the repo owner as a `good first issue` — was about to be labelled `status: needs-format` and answered in public with "Thanks for filing this… edit the description", making the issue look unready to the newcomers it was opened for; and the keyword classifier read "the static-site gate" as `area: flight-engine` and would have removed the maintainer's correct `area: community` to make room (2026-09-22) | the template gate is for REPORTERS: an issue the repo owner authored is exempt, the same way epics already were, and an unresolved owner exempts nobody so the gate can only ever stay ON. And a single `area:`/`priority:` label already on an issue is a person's call — the classifier fills a family nobody has decided, and still breaks the tie when a family contradicts itself, which is what supersession was for | `issue-triage.ts` (`isMaintainerAuthored`, `repoOwnerOf`, `fetchRepoOwner`, `handSetFamilyLabel`), `test/flight/issue-triage-protocol.test.ts` |
| 47 | A check whose header promised more than its code did. `ci:model-freshness` documented two kinds of staleness — an unknown family, and a pinned id no longer current — and implemented only the first, reading family aliases off `claude --help` for free. It reported OK the day Opus 5.5 shipped while the catalogue still pinned `claude-opus-5` and the `opus` alias already resolved to `claude-opus-5-5`; the installed CLI was also too old to fly it (2026-09-24) | the pinned case has an implementation, behind `--probe`: one real one-word call per family reads the concrete id off the reply and is compared to the pin, opt-in because it costs money. Without `--probe` the check says the pins were NOT looked at instead of implying they were. A promise in a doc comment is a claim the code has to keep | `scripts/ci/check-model-freshness.mjs` (`cataloguePinnedIds`, `resolveAliasFromUsage`, `findStalePins`), `docs/MODELS.md` |

Row 28 is row 19-20's lesson wearing different clothes, and that is the
part worth keeping: **the same mistake relocates.** Publishing the
ritual's internal monologue was fixed in GitHub comments and then
reappeared in commit messages weeks later, because the fix addressed the
SURFACE and not the habit. The habit is writing for the author instead
of the reader. A commit message says what changed and why; whose machine
died, at what o'clock, is a debrief detail — and debriefs already exist
for exactly that.

Row 28's second lesson is the sharper one, and it cost a second leak to
learn: **the first version of that rule checked only the SUBJECT, and the
very commit that introduced it repeated the private detail in its own
BODY — straight past the new guard.** A guard covering half its surface
is how the thing it forbids comes back. The rule now reads the whole
message, and the first test in its suite is the body case.

Row 26 came with a second lesson about guards themselves. The obvious
check — "does a parent have lines the merge lacks?" — flagged **14 of
this repo's 90 merges**, every one a false positive: the fleet's
sync-back legitimately produces identical-tree merges when a lane's work
already landed by another route. `git cherry` could not adjudicate it
either (the merge makes the parent reachable, so every commit reads as
"applied" even when its content was dropped). Only the tip-content test
separates the two. **A guard is not finished when it fires; it is
finished when it stops firing on the things that are fine.**

Row 25 is the shape worth naming on its own: **a fix applied to half its
surface reads as a fix until the other half fails.** The comment above
`testTimeout` had already diagnosed the exact cause (real-git suites,
loaded Windows CI) — the setup cost simply lived in hooks the setting
never reached.

Board hygiene has its own auditor: `pnpm audit:board-flood` sweeps every
thread for near-duplicates, consecutive runs past the ceiling, and
rapid-fire posts. It found rows 19 and 20 across 31 threads; it is what
proves the guard is working rather than the guard proving itself.

Row 8 closed 2026-09-09, and its shape is worth keeping: the ritual had a
hook named `postPushWatch` sitting directly beneath a merge that never
pushed. **A name can carry an assumption the code never honoured**, and
nothing failed loudly enough to notice — the operator found it by asking
which button publishes.

Rows 10 (reland), 14 (structural), 16, 27 (operator-owned) are the open
counters — each is a boarded task or a standing operator decision;
everything else is live machinery. When one ships, move its row's
"where" to the code path in the same commit.
