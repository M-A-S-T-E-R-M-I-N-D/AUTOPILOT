# AUTOPILOT — pnpm configuration
# Keep peer resolution lenient so a clean-machine install never fails on a
# transitive peer mismatch; the lockfile still pins exact versions (SLSA).
strict-peer-dependencies=false
auto-install-peers=true
# Native modules (better-sqlite3) build/link deterministically under pnpm.
node-linker=isolated
