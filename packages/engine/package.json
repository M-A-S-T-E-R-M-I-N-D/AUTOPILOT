{
  "name": "@autopilot/engine",
  "version": "0.1.0",
  "private": true,
  "description": "AUTOPILOT engine — the gated autonomous loop (orient → pick → gate → commit → report → pace).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@autopilot/store": "workspace:*"
  }
}
